# Root catch-all — each subfolder also has its own .gitignore.
.DS_Store
*.log
node_modules
.env
.env.local
.env.*.local
!**/.env.example
!**/.env.local.example
.vercel
.next
dist
.expo

# Claude Code personal permission grants (machine-local, not committed)
.claude/settings.local.json

# Written by scripts/ota.sh while app.json is mid-swap.
.ota-running
