# Palate code review — 2026-09-05

Two review passes (six then twelve subsystems) plus an adversarial pass on the "Not interested" feature. Pass-one findings were never verified (the run died on a session limit); pass-two severity 4–5 findings were verified by a refute lens and a fix lens. The synthesized report from pass two is appended at the end when it lands.

Totals: pass 1 98 findings, pass 2 225, attack pass 12 confirmed / 17 refuted.

## Every finding, by severity

| sev | pass | file:line | category | effort | summary | status |
|---|---|---|---|---|---|---|
| 5 | pass 2 | `mobile/app/featured-list/[slug].tsx:58` | correctness | S | Conditional hook: the `if (loading) return` at line 58 sits above the useEffect at line 70, so the Friday/Saturday discovery ping's tap dest | fixed hook order |
| 5 | pass 2 | `mobile/app/featured-list/[slug].tsx:58` | correctness | S | Hooks-order violation: an early return at line 58 sits between two useEffect calls, so the Featured List screen throws whenever it has to lo | fixed hook order |
| 5 | pass 1 (unverified) | `mobile/app/friends.tsx:46` | correctness | M | The Board crash: listFriends() embeds `profiles` through an own-row RLS policy, so the other person in every accepted friendship comes back  | fixed 0102 list_friendships RPC |
| 5 | pass 1 (unverified) | `mobile/app/group.tsx:127` | correctness | S | The "Eat together" chip on the Feed crashes the same way: group.tsx renders `f.friend.id` / `f.friend.avatar_url` for every listFriends() ro | fixed 0102 + defensive render |
| 5 | pass 1 (unverified) | `mobile/lib/featured-lists.ts:86` | cost | M | Users without a city override are keyed as a fresh 'city' per 0.01° GPS cell, and every cell is registered for the nightly Google Text Searc | fixed 0103 + coarse cells + weekly refresh |
| 5 | pass 1 (unverified) | `mobile/lib/profile.ts:43` | correctness | S | Edit Profile loads bio/school/first/last/Instagram/TikTok as blank and "Save profile" then overwrites all six with null. | fixed 6f6a… Edit Profile fields selected |
| 5 | pass 2 | `supabase/02_policies.sql:17` | security-privacy | S | Any signed-in user can make themselves an admin by PATCHing their own profiles row | fixed 0101 |
| 5 | pass 2 | `supabase/functions/classify-cuisine-backfill/index.ts:57` | cost | S | The Haiku backfill re-selects the same rows every 10 minutes forever; a one-time ~$0.50 pass becomes a permanent 500-calls/day loop. | fixed llm_backfill_at |
| 5 | pass 2 | `supabase/functions/gmail-import/index.ts:612` | cost | M | Every Google Places call the Gmail import spends on a NEW restaurant is wasted — the place is never inserted into `restaurants`, so the visi | fixed minimal row insert |
| 5 | pass 1 (unverified) | `supabase/migrations/0001_baseline.sql:220` | security-privacy | S | Any signed-in user can set is_admin=true on their own profile row and unlock every admin RPC, including the server_push switch. | fixed 0101 is_admin guard |
| 5 | pass 2 | `supabase/migrations/0077_open_feed.sql:75` | security-privacy | S | feed_events is readable by the anon key — anyone can see who ate where | fixed 0104 restrictive policy |
| 4 | pass 2 | `.github/workflows/build.yml:98` | testing | S | The smoke-test job cannot run: supabase/config.toml is not tracked by git, so `supabase db start` has no project config in a clean checkout. | fixed config.toml added |
| 4 | pass 2 | `.github/workflows/build.yml:98` | testing | S | The `supabase` CI job cannot run: there is no supabase/config.toml in the repo, and `supabase db start` requires one. | fixed config.toml added |
| 4 | pass 2 | `.github/workflows/build.yml:67` | correctness | S | Nothing enforces the documented rule that a native change must bump app.json `version`, which is the runtime an OTA targets. |  |
| 4 | pass 1 (unverified) | `mobile/app/(tabs)/discover.tsx:51` | retention-product | M | The whole Discover tab is built from a single 20-result Google Nearby call, so after chain and already-visited filtering the Most Compatible | fixed 0109 restaurants_near merged |
| 4 | pass 2 | `mobile/app/(tabs)/discover.tsx:555` | retention-product | S | Every return to Discover replaces the ranked list with shimmer skeletons, unmounting every card and losing scroll position. |  |
| 4 | pass 2 | `mobile/app/(tabs)/discover.tsx:213` | cost | S | load() depends on the browsingCity object, so a user with a city picked runs the whole Discover pipeline twice on mount. |  |
| 4 | pass 2 | `mobile/app/_layout.tsx:437` | retention-product | S | The notification-primer route guard hijacks onboarding at the /import-email step and dumps the new user into the tabs | fixed primer on tabs only |
| 4 | pass 2 | `mobile/app/_layout.tsx:119` | correctness | S | A font-loading failure hangs the app on the splash spinner forever, with no error boundary and no report | fixed font error proceeds |
| 4 | pass 2 | `mobile/app/notifications-intro.tsx:97` | retention-product | M | The notification primer's first and strongest reason is a push that never sends |  |
| 4 | pass 2 | `mobile/app/onboarding/permission.tsx:61` | accessibility | M | Ten full-screen onboarding/auth screens have no ScrollView, so the CTA is unreachable at large text |  |
| 4 | pass 2 | `mobile/app/passive-capture-intro.tsx:78` | retention-product | S | passive-capture-intro fires the cold iOS notification dialog, defeating the primer that exists to prevent exactly that | not taken: contextual ask, primer skips when granted |
| 4 | pass 2 | `mobile/app/passive-capture-intro.tsx:78` | retention-product | S | Onboarding fires the cold iOS notification dialog at passive-capture-intro.tsx:78, burning the one ask before the primer screen that exists  | not taken: contextual ask, primer skips when granted |
| 4 | pass 2 | `mobile/app/sign-in.tsx:95` | retention-product | M | Onboarding is entered from exactly one code path, and hasCompletedOnboarding() fails closed — one network blip at signup skips it permanentl | fixed account-age rule |
| 4 | pass 2 | `mobile/components/RecommendationsCard.tsx:274` | retention-product | S | The core Home recommendation card renders nothing at all when its load fails |  |
| 4 | pass 2 | `mobile/lib/auto-detect.ts:69` | cost | M | Every app foreground fires an uncacheable billed Google Nearby call before anything is known to be nearby | fixed nearby cache |
| 4 | pass 1 (unverified) | `mobile/lib/friends.ts:152` | security-privacy | S | All three friendship embeds (lines 152-153, 179, 226) and listBlockedUsers (moderation.ts:79) still select `email` from profiles, and friend | fixed 0102 (no email) |
| 4 | pass 1 (unverified) | `mobile/lib/moderation.ts:79` | correctness | S | Blocked accounts screen embeds profiles via PostgREST, but the only profile SELECT policy is own-row, so every blocked person renders as "Un | fixed 0102 list_blocked RPC |
| 4 | pass 1 (unverified) | `mobile/lib/mood.ts:183` | correctness | S | An empty nearby pool makes applyMood report matched:true, so a catalogue-offered cuisine or dish chip never reaches the catalogue in exactly | see attack pass; catalogue fallback |
| 4 | pass 2 | `mobile/lib/nearby-cache.ts:60` | cost | S | getOrFetchNearby has no in-flight de-duplication, so two concurrent callers for the same bucket both bill Google Places. |  |
| 4 | pass 2 | `mobile/lib/notifications.ts:91` | correctness | M | "Your Palate Wrapped is ready" fires Sunday 09:00 in the device's LOCAL zone, but the Wrapped is generated at a fixed 14:00 UTC — so for eve | fixed Sunday 10:00 |
| 4 | pass 1 (unverified) | `mobile/lib/palate/palateScoring.ts:289` | correctness | M | 'New place' is computed within the 7-day window, so a person who visits four different regulars once each per week is classified Forager and | fixed new-place rate against history |
| 4 | pass 1 (unverified) | `mobile/lib/passive-capture.ts:157` | security-privacy | S | Turning the remote kill switch OFF never stops monitoring on a device that already started it: the native manager re-arms continuous backgro | fixed kill switch stops native monitor |
| 4 | pass 1 (unverified) | `mobile/lib/passive-confirm.ts:35` | retention-product | S | INBOX_EXPIRY_HOURS=24 is shorter than the digest cycle, so a dinner detected after 21:00 is announced by the next night's digest notificatio | fixed inbox 48h |
| 4 | pass 1 (unverified) | `mobile/lib/passive-pipeline.ts:397` | correctness | S | When the Google daily cap is tripped, places-proxy returns HTTP 200 with degraded results that exclude every eligibility-0 venue (chains, fa | fixed degraded → retry later |
| 4 | pass 1 (unverified) | `mobile/lib/passive-runner.ts:109` | correctness | S | A failed read of passive_capture_resolve (or _confirm) is treated as a successful 'flag off' outcome and the visit is marked processed forev | fixed tri-state flag read + retry |
| 4 | pass 2 | `mobile/lib/persona-prior.ts:82` | retention-product | M | Taking the onboarding quiz makes cold-start scores worse, not better — four of nine personas drive every restaurant to the score floor. | fixed scoreTaste gate |
| 4 | pass 2 | `mobile/lib/personal-signal.ts:77` | security-privacy | S | loadPersonalSignal writes cacheUserId but never reads it, and signOut never invalidates — a second account in the same session inherits the  | fixed caches cleared on auth change |
| 4 | pass 2 | `mobile/lib/recommendation/compatibility.ts:144` | correctness | M | Scores normalize over only the attributes a venue happens to have, so an unclassified place outranks a correctly-classified place the user w | already: confidence weighting earlier today |
| 4 | pass 2 | `mobile/lib/recommendation/index.ts:59` | correctness | M | The compatibility cache is keyed on google_place_id alone, so the first surface to score a place fixes the % for every other surface regardl | partial: cache key includes dislikes |
| 4 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:297` | correctness | S | The departure-path emit takes a fresh high-accuracy fix at the place the user just left FOR, and stamps it on the old stop | needs founder: native build |
| 4 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:369` | cost | S | The passive_capture_detection kill switch stops the JS pipeline but never stops the native monitor | fixed on the JS side (stopMonitoring on flag off) |
| 4 | pass 2 | `supabase/02_policies.sql:42` | security-privacy | M | Any signed-in user can rewrite or blacklist any of the 1066 shared restaurant rows | fixed 0104 restaurants policies dropped |
| 4 | pass 2 | `supabase/functions/_shared/classifier.ts:1001` | retention-product | S | The "nightlife" gate excludes ordinary restaurants, because the `party` tag it keys on is produced deterministically by any place with `bar` | fixed party removed from bars (new rows) |
| 4 | pass 2 | `supabase/functions/classify-cuisine-backfill/index.ts:106` | cost | S | A model call that throws is billed but never counted against LLM_DAILY_CAP, and temperature 0 makes the failure permanent. | fixed usage counted before call |
| 4 | pass 2 | `supabase/functions/classify-cuisine-backfill/index.ts:93` | cost | M | The Haiku cuisine backfill writes into a column that free nearby/search upserts overwrite with null, so the */10 cron will pay for the same  | fixed 0104 preserve trigger |
| 4 | pass 2 | `supabase/functions/featured-lists-refresh/index.ts:285` | correctness | S | `void admin.from("restaurants").upsert(...)` never issues an HTTP request — postgrest-js builders only execute inside `then()`, so the 'keep | fixed await |
| 4 | pass 2 | `supabase/functions/featured-lists-refresh/index.ts:144` | cost | M | Any authenticated account — including an unapproved waitlist signup — can spend the entire daily Google budget through refresh_city with arb | fixed registered-city gate |
| 4 | pass 2 | `supabase/functions/generate-weekly-wrapped/index.ts:89` | retention-product | S | The Sunday Wrapped cron is scheduled and pushes directly to Expo, ignoring server_push, quiet hours and every notification preference | fixed server_push gate |
| 4 | pass 2 | `supabase/functions/notify-feed-post/index.ts:99` | retention-product | S | Pushes straight to Expo, bypassing the server_push kill switch, quiet hours, the daily cap and the user's push_social_activity preference | fixed server_push gate |
| 4 | pass 2 | `supabase/functions/notify-feed-post/index.ts:99` | retention-product | M | notify-feed-post posts straight to Expo with no server_push check, no quiet hours, no push_social_activity opt-out, no daily cap and no outb | fixed server_push gate |
| 4 | pass 1 (unverified) | `supabase/functions/places-proxy/index.ts:157` | cost | S | The proxy's per-user nearby rate limit counts location_events rows that nothing ever inserts, and search has no per-user limit at all, so on | fixed 0106 proxy_calls per-user cap |
| 4 | pass 2 | `supabase/functions/places-proxy/index.ts:535` | correctness | M | Every nearby/search/featured-lists/reclassify upsert overwrites the LLM-derived columns with nulls | fixed 0104 preserve trigger |
| 4 | pass 2 | `supabase/functions/places-proxy/index.ts:125` | correctness | M | Nothing in the app calls action "details", so the LLM classifier, the review miner and the atmosphere attributes never run | open: details action unused (design) |
| 4 | pass 2 | `supabase/functions/places-proxy/index.ts:157` | cost | S | The per-user rate limit on the most expensive endpoint counts a table nothing writes, so there is no per-user cap on Google spend at all | fixed 0106 proxy_calls per-user cap |
| 4 | pass 2 | `supabase/functions/places-proxy/index.ts:42` | cost | S | Setting ANTHROPIC_API_KEY for the approved cuisine backfill silently arms two uncapped, unmetered LLM paths in the user-facing proxy | fixed LLM meter 300/day |
| 4 | pass 2 | `supabase/functions/places-proxy/index.ts:248` | data-quality | M | Bulk nearby/search upserts overwrite every enrichment column with NULL and refresh the cache stamps, so paid details+LLM work is destroyed a | fixed 0104 preserve trigger |
| 4 | pass 2 | `supabase/functions/reclassify/index.ts:123` | cost | S | reclassify erases what classify-cuisine-backfill paid Haiku to produce, and the two then re-do each other's work forever | fixed by 0104 preserve trigger |
| 4 | pass 2 | `supabase/functions/reclassify/index.ts:115` | cost | S | reclassify cannot advance past a row whose Google fetch fails, so the sweep stalls and re-pays for the same head every run | fixed cursor advances on failure |
| 4 | pass 2 | `supabase/functions/send-push/index.ts:136` | retention-product | S | The one-proactive-push-per-user-per-day cap does not count sends made within the same run, so a user with several queued rows gets all of th | not real: seenThisRun counts same-run sends |
| 4 | pass 2 | `supabase/functions/send-push/index.ts:81` | retention-product | S | The server_push gate returns before the expiry sweep, so nothing ever ages out of push_outbox while the switch is off — flipping it on deliv | fixed expiry before gate |
| 4 | pass 1 (unverified) | `supabase/migrations/0001_baseline.sql:337` | security-privacy | M | "Delete my account" leaves every meal photo and avatar publicly downloadable and never revokes the Gmail grant at Google. |  |
| 4 | pass 1 (unverified) | `supabase/migrations/0007_social_layer.sql:61` | security-privacy | S | A blocked user can re-send a friend request to the person who blocked them: the friendships INSERT policy only checks `auth.uid() = requeste | fixed 0108 insert policy |
| 4 | pass 2 | `supabase/migrations/0007_social_layer.sql:64` | security-privacy | S | The friendships UPDATE policy lets an addressee rewrite requester_id and forge an accepted friendship | fixed 0104 parties immutable |
| 4 | pass 2 | `supabase/migrations/0022_gmail_integration.sql:52` | correctness | S | gmail_connection_status() raises 42702 for every caller — the Gmail screen has never worked | fixed 0104 42702 |
| 4 | pass 2 | `supabase/migrations/0024_featured_lists_cache.sql:120` | security-privacy | S | No view in the schema sets security_invoker, so views bypass RLS — featured_lists_for_city serves the whole paid cache to anon | fixed 0104 view revoked from anon |
| 4 | pass 2 | `supabase/migrations/0033_cost_controls.sql:94` | security-privacy | S | bump_google_usage and record_api_usage are revoked from `public` only, not from `anon`/`authenticated` by name | fixed 0104 grants by name |
| 4 | pass 2 | `supabase/migrations/0061_remove_waitlist_gate.sql:64` | correctness | S | The join-broadcast trigger can insert NULL into push_outbox.send_after (NOT NULL), which would abort every new signup. | not real: broadcast_recipients requires timezone |
| 4 | pass 1 (unverified) | `supabase/migrations/0088_snapshot_friend_state.sql:75` | security-privacy | S | Blocking does not stop the blocked person from reading you: get_friend_profile_snapshot, friend_taste_features, shared_places and search_use | fixed 0108 blocks cut both ways |
| 4 | pass 2 | `supabase/migrations/0099_dish_family.sql:127` | testing | S | A data-dependent assertion (`if n < 400`) makes the migration fail on any empty database, so from-scratch rebuild can never be green. | fixed relative assertion |
| 4 | pass 2 | `supabase/tests/smoke.sql:60` | testing | S | friends_leaderboard is plpgsql, was rewritten in 0098, and no call anywhere proves its body parses — the exact 42702 shape smoke.sql exists  | fixed smoke covers every reader |
| 4 | pass 2 | `supabase/tests/smoke.sql:69` | security-privacy | M | The anon-denial loop hardcodes 5 function names out of 46 security-definer functions; 8 of the unpinned ones answer the anon key with HTTP 2 |  |
| 4 | pass 2 | `supabase/tests/smoke.sql:51` | correctness | M | smoke.sql executes 5 of 46 definer functions, and misses 4 of the 5 plpgsql `return query` functions it exists to protect — including friend |  |
| 3 | pass 2 | `.github/workflows/build.yml:38` | correctness | S | The 10 Deno edge functions are never type-checked or compiled anywhere in CI. |  |
| 3 | pass 2 | `.github/workflows/build.yml:56` | testing | S | CI type-checks and tests the app but never bundles it, so app.json plugin errors and Metro-only failures are first discovered by a paid EAS  |  |
| 3 | pass 2 | `mobile/.env.example:7` | correctness | S | .env.example lists 2 of the 4 EXPO_PUBLIC_ vars the app reads; the two missing ones fail silently, and one of them is the Sentry DSN. |  |
| 3 | pass 2 | `mobile/app.json:20` | security-privacy | S | NSLocationWhenInUseUsageDescription tells the user location is used 'only when the app is open' while the app runs continuous background loc |  |
| 3 | pass 2 | `mobile/app/(tabs)/_layout.tsx:56` | accessibility | S | Tab bar labels bypass components/Text (so they are not Inter) and scale uncapped inside a capped bar |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/discover.tsx:244` | cost | S | Every search submit is a Google Text Search, even though the 100 most-reviewed restaurants for the city are already loaded in memory and res |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/discover.tsx:167` | retention-product | S | Discover refuses to show anything when GPS accuracy is worse than 200m, which is a visit-logging threshold applied to a 2.5km browse query. |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/discover.tsx:157` | retention-product | S | Every switch to the Discover tab blanks the list to a skeleton and waits for a fresh GPS fix plus four queries before showing content again. |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/discover.tsx:879` | correctness | S | The filter labelled 'Only places I've saved' does not filter to saved places; it re-ranks the same nearby pool using a taste vector rebuilt  |  |
| 3 | pass 2 | `mobile/app/(tabs)/discover.tsx:581` | retention-product | S | Discover shows an error card and a 'log a few visits' empty state at the same time on failure |  |
| 3 | pass 2 | `mobile/app/(tabs)/discover.tsx:210` | code-health | S | Raw exception messages are rendered to the user in Discover's error card |  |
| 3 | pass 2 | `mobile/app/(tabs)/discover.tsx:248` | retention-product | S | A failed restaurant search tells the user the restaurant does not exist |  |
| 3 | pass 2 | `mobile/app/(tabs)/discover.tsx:208` | data-quality | S | Every Discover focus fires 12 separate auth calls and 12 separate analytics INSERTs, and re-counts the same impressions. |  |
| 3 | pass 2 | `mobile/app/(tabs)/feed.tsx:124` | performance | M | Up to 60 feed cards render in a plain ScrollView with no virtualization. |  |
| 3 | pass 2 | `mobile/app/(tabs)/feed.tsx:100` | performance | S | One Kudos tap re-renders all 60 feed rows because FeedRow is unmemoized and onLike is a fresh closure per render. |  |
| 3 | pass 2 | `mobile/app/(tabs)/feed.tsx:66` | performance | M | The taste graph lands after first paint and triggers up to 60 uncached compatibility computations inside the render phase. |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/index.tsx:108` | correctness | S | 'Surprise me' is offered to users with only single-visit cuisines, but habitualCuisines for those users is empty, so the chip returns the sa |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/index.tsx:214` | performance | S | friendCount and celebratedStreak are load() dependencies, so every cold mount runs the full Home load twice (three times on a milestone day) |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/index.tsx:263` | retention-product | S | Pull-to-refresh and re-focus never refresh the picks or the chips, and the card's scoring location is frozen at first mount. |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/wrapped.tsx:352` | correctness | S | The same Wrapped row is shown with two different date ranges on one screen ('Aug 30 – Sep 6' in the THIS WEEK block, 'Aug 31 to Sep 7' on th |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/wrapped.tsx:91` | data-quality | S | The identity, tags and story numbers come from a rolling last-7-days vector while the THIS WEEK block, share-card stats/range and feed post  |  |
| 3 | pass 1 (unverified) | `mobile/app/(tabs)/wrapped.tsx:118` | correctness | S | The all-time hero identity is smoothed 70/30 against the WEEK's stored scores (no useSmoothing:false), and the story computes its own profil |  |
| 3 | pass 2 | `mobile/app/_layout.tsx:294` | retention-product | S | Tapping the Sunday Wrapped or streak-reminder notification opens Home instead of what the notification promised |  |
| 3 | pass 2 | `mobile/app/_layout.tsx:422` | correctness | M | Three writers race for the post-sign-in route; the guard mounts Home for brand-new accounts before onboarding wins |  |
| 3 | pass 2 | `mobile/app/_layout.tsx:294` | retention-product | S | Five notification types have no branch in the response handler and fall through to `if (data?.kind !== "passive_confirm") return`, so the ta |  |
| 3 | pass 2 | `mobile/app/confirm-visit.tsx:209` | retention-product | S | The medium-confidence prompt asks a two-way question but offers no button for the second answer |  |
| 3 | pass 2 | `mobile/app/digest.tsx:230` | accessibility | S | The evening digest's confirm checkboxes have no role, state, or label |  |
| 3 | pass 1 (unverified) | `mobile/app/friends.tsx:73` | correctness | S | `loadError` is set on failure but never rendered, so a failed listFriends/listIncomingRequests/listOutgoingRequests shows "No friends yet",  |  |
| 3 | pass 2 | `mobile/app/index.tsx:5` | correctness | S | Every cold start of a signed-in user renders the sign-in screen for a frame before the guard bounces it |  |
| 3 | pass 1 (unverified) | `mobile/app/insights.tsx:369` | retention-product | M | Two identity systems are live on two screens: Wrapped names the user Curator/Forager/Steward/Anchor while Profile → Insights names them 'Lat |  |
| 3 | pass 1 (unverified) | `mobile/app/map.tsx:150` | cost | S | Opening the map with a city override fires two concurrent fetchAt calls for the same coordinates (initialLoad + the browsingCity effect). |  |
| 3 | pass 1 (unverified) | `mobile/app/map.tsx:23` | cost | S | Four different nearby radii across surfaces (2500 / 3000 / 4000→clamped 3000) defeat the proxy's per-cell cache, which is keyed on radius. |  |
| 3 | pass 1 (unverified) | `mobile/app/map.tsx:242` | performance | M | tracksViewChanges is a no-op on Apple Maps, so the numbered-vs-dot optimisation does nothing and all 60+ markers, including 13 with looping  |  |
| 3 | pass 2 | `mobile/app/map.tsx:179` | performance | S | placesArr, topScore and numbered are recomputed and the whole marker list re-created on every render, including every marker tap. |  |
| 3 | pass 2 | `mobile/app/map.tsx:82` | performance | M | The map's places Map is never pruned, so markers accumulate without bound as the user pans. |  |
| 3 | pass 2 | `mobile/app/notifications-intro.tsx:113` | retention-product | S | The primer promises "Sunday Wrapped" and then never schedules it — enableSundayWrappedReminder is only ever called from the Settings switch. |  |
| 3 | pass 2 | `mobile/app/notifications-intro.tsx:97` | retention-product | S | The primer's headline reason to grant permission — "A friend just ate somewhere" — cannot be delivered today, because server_push is false a |  |
| 3 | pass 2 | `mobile/app/notifications-intro.tsx:78` | retention-product | S | The permission screen promises 'nothing arrives late at night' and a 9pm digest; Fri/Sat fire at 11pm |  |
| 3 | pass 2 | `mobile/app/onboarding/permission.tsx:28` | retention-product | L | Roughly 20 taps and 9 screens from install to background tracking on, with the core feature at step 8 |  |
| 3 | pass 2 | `mobile/app/onboarding/permission.tsx:35` | correctness | S | Onboarding dead-ends when the location permission status is neither granted nor denied |  |
| 3 | pass 2 | `mobile/app/profile/[id].tsx:21` | accessibility | S | 26 back/close buttons are a bare arrow glyph in a Pressable; one has a label, three have a role |  |
| 3 | pass 2 | `mobile/app/settings.tsx:502` | accessibility | S | All six Settings switches are unnamed to VoiceOver |  |
| 3 | pass 2 | `mobile/app/waitlist.tsx:19` | correctness | S | 'Check my status' silently does nothing when the approval check throws |  |
| 3 | pass 1 (unverified) | `mobile/app/wrapped-story.tsx:341` | data-quality | S | Story card 5 renders fabricated cohort statistics ('1,050 Palates eat like you. They average 4.1 eating-out meals a week.') as fact, with no |  |
| 3 | pass 1 (unverified) | `mobile/components/FeaturedLists.tsx:38` | cost | S | buildFeaturedLists re-runs on every Discover focus and every 'saves only' toggle because unused `vector`/`personal` objects are in the effec |  |
| 3 | pass 2 | `mobile/components/FeaturedLists.tsx:38` | cost | S | buildFeaturedLists re-runs on every Discover focus because two deps are fresh object identities, and each run can fire a full uncoordinated  |  |
| 3 | pass 2 | `mobile/components/HypeMap.tsx:80` | retention-product | S | HypeMap reserves a 304pt placeholder and then returns null, jumping the whole feed up mid-read. |  |
| 3 | pass 1 (unverified) | `mobile/components/ProfileBody.tsx:81` | correctness | S | A failed get_friend_profile_snapshot call is logged to console.warn and rendered as "Profile not found." — a claim about the person, not a r |  |
| 3 | pass 1 (unverified) | `mobile/components/RecommendationsCard.tsx:88` | correctness | S | dish_family is never present on the nearby pool, so every dish chip misses the in-pool fast path and goes through the catalogue RPC with a ' |  |
| 3 | pass 1 (unverified) | `mobile/components/RecommendationsCard.tsx:201` | cost | S | With a browsing city set, the card's load() runs twice on mount and both calls can reach Google, doubling the billable nearby call for that  |  |
| 3 | pass 1 (unverified) | `mobile/components/RecommendationsCard.tsx:253` | correctness | S | catalogueLoading is never reset when a catalogue fetch is superseded by a non-catalogue mood, leaving 'Looking further out for Anything…' on |  |
| 3 | pass 2 | `mobile/components/RecommendationsCard.tsx:370` | retention-product | S | Home, the feed, the map and StretchPick print a precise "% match" at low confidence; the detail screen hides the same number, so tapping a c |  |
| 3 | pass 2 | `mobile/components/RecommendationsCard.tsx:156` | cost | S | Home requests nearby at 3000m while every other surface uses 2500m, doubling the billed Google Places nearby calls per location. |  |
| 3 | pass 2 | `mobile/components/RestaurantCompatibilityCard.tsx:90` | retention-product | S | "Show less like this" / dismiss never busts the compatibility cache, so the dismissed place comes straight back at the same score. |  |
| 3 | pass 2 | `mobile/components/UsernameField.tsx:63` | accessibility | S | Four text inputs have a fixed pixel height and uncapped font scaling, including the mandatory signup field |  |
| 3 | pass 1 (unverified) | `mobile/components/WrappedCharts.tsx:32` | data-quality | S | WrappedCharts fetches week analytics once on mount while the tab refreshes on every focus, so after logging a visit the THIS WEEK block upda |  |
| 3 | pass 2 | `mobile/lib/__tests__/match-confidence.test.ts:1` | testing | M | Every scoring test targets the scorer nothing calls; computeCompatibility, scoreRestaurant, computeRightNow and generateCandidates have zero |  |
| 3 | pass 2 | `mobile/lib/a11y.ts:44` | accessibility | M | FONT_CAP is defined for five roles and applied on 17 of 1006 Text render sites |  |
| 3 | pass 1 (unverified) | `mobile/lib/auto-detect.ts:53` | cost | S | The foreground auto-detect also runs when iOS relaunches the app in the background, makes an uncached paid Google call on nearly every foreg |  |
| 3 | pass 1 (unverified) | `mobile/lib/friends.ts:207` | security-privacy | S | LeaderboardEntry still types `email` and friends.tsx:222/225 still render it; whether the live friends_leaderboard is the 0098 (email null)  |  |
| 3 | pass 2 | `mobile/lib/location.ts:23` | performance | S | Every Discover focus blocks the whole screen behind a cold GPS fix with no last-known-position fast path. |  |
| 3 | pass 1 (unverified) | `mobile/lib/moderation.ts:79` | correctness | S | The Blocked screen cannot show who you blocked: listBlockedUsers embeds `profiles` through the same own-row RLS, so every blocked person ren | fixed 0102 list_blocked RPC |
| 3 | pass 1 (unverified) | `mobile/lib/nearby-cache.ts:56` | cost | S | Discover runs load() twice on mount whenever a browsing city is set, and nearby-cache has no in-flight dedupe, so two identical paid nearby  |  |
| 3 | pass 2 | `mobile/lib/nearby-cache.ts:60` | cost | S | No in-flight deduplication, so concurrent callers on the same screen each bill for the same bucket despite the cache |  |
| 3 | pass 1 (unverified) | `mobile/lib/next-step.ts:64` | retention-product | S | A connected mailbox with zero committed imports pins the Home hero to 'Your email is connected. See what's in it.' forever, above the locati |  |
| 3 | pass 2 | `mobile/lib/notification-schedule.ts:21` | retention-product | M | The documented "one nudge per day, max" invariant is false — Saturday carries four local notifications — and the test that guards it only lo |  |
| 3 | pass 2 | `mobile/lib/notifications.ts:257` | correctness | S | disableSundayWrappedReminder still cancels by remembered id, so the dedupe-by-kind fix does not cover the OFF path — an orphaned Sunday remi |  |
| 3 | pass 2 | `mobile/lib/notifications.ts:213` | correctness | S | Only refreshDiscoveryPings is serialize()-wrapped; refreshDailyReminder and refreshWrappedTease are re-entered by Home's focus effect and ca |  |
| 3 | pass 2 | `mobile/lib/notifications.ts:251` | retention-product | S | The 20:00 nudge fires nightly for accounts with zero visits, using copy about a Wrapped they have never had, and Settings offers no way to t |  |
| 3 | pass 2 | `mobile/lib/observability.ts:46` | correctness | S | captureError silently drops everything until the async Sentry init resolves — precisely the launch window it exists to observe |  |
| 3 | pass 2 | `mobile/lib/observability.ts:10` | correctness | M | No EXPO_PUBLIC_SENTRY_DSN exists anywhere in the tree, so shipped bundles likely have observability fully disabled |  |
| 3 | pass 1 (unverified) | `mobile/lib/palate-labels.ts:387` | correctness | S | When no archetype rule scores above 0.35, both the primary identity and the THIS WEEK mood fall to 'Just Getting Started' — the zero-visit c |  |
| 3 | pass 1 (unverified) | `mobile/lib/palate-labels.ts:437` | correctness | S | Palate Lore is keyed by the label's last word, so 'Comfort Seeker' gets the Variety-Seeker lore ('Familiar isn't the goal. The search is.')  |  |
| 3 | pass 1 (unverified) | `mobile/lib/palate/palateScoring.ts:209` | correctness | S | The 'prior week' used for smoothing, movement copy and the axis arrow is overwritten on every Wrapped tab open, so 'You moved toward Curator |  |
| 3 | pass 1 (unverified) | `mobile/lib/palate/palateScoring.ts:49` | retention-product | S | Three different unlock numbers: copy promises Wrapped at 3 visits, the row generates at 1, and the identity stays 'Learning' below 4 visits  |  |
| 3 | pass 1 (unverified) | `mobile/lib/passive-capture.ts:177` | correctness | S | The JS raw-visit queue is never trimmed, so once 1000 ids have been processed every run re-runs the oldest visits as if new (Google call, cl |  |
| 3 | pass 1 (unverified) | `mobile/lib/passive-digest.ts:243` | correctness | S | The digest notification's title and body are built from the window at scheduling time, but the screen it opens builds the window at open tim |  |
| 3 | pass 2 | `mobile/lib/passive-digest.ts:187` | correctness | S | The Friday and Saturday digest fires at 23:00, inside the quiet window every other part of the system enforces, and contradicts the primer's |  |
| 3 | pass 1 (unverified) | `mobile/lib/passive-pipeline.ts:376` | cost | M | Every qualified passive detection is a billable Google searchNearby: the client cache cannot hit by construction and the server read-through |  |
| 3 | pass 1 (unverified) | `mobile/lib/passive-runner.ts:87` | data-quality | S | recordForClustering runs on every retry attempt, so one visit that fails resolution three times writes three cluster points and can permanen |  |
| 3 | pass 2 | `mobile/lib/recommendation/compatibility.ts:231` | retention-product | M | Per-restaurant loved / not-for-me ratings never affect that restaurant's score — the graph field is populated and read by nothing but the ca |  |
| 3 | pass 2 | `mobile/lib/recommendation/explanations.ts:46` | data-quality | S | "N friends have visited" counts friend visit ROWS, not distinct friends — one friend who went four times reads as four friends. |  |
| 3 | pass 2 | `mobile/lib/recommendation/index.ts:78` | performance | S | buildRankedRestaurant computes compatibility twice per restaurant — once through the cache, once around it. |  |
| 3 | pass 2 | `mobile/lib/recommendation/right-now.ts:192` | retention-product | M | The "stretch your palate" card picks the LOWEST-compatibility candidate from a pool that includes almost everything, because scoreNovelty la |  |
| 3 | pass 2 | `mobile/lib/recommendation/taste-graph.ts:72` | retention-product | M | Wishlist saves — the most explicit intent signal the app collects — are dropped on the floor by assembleGraph and never reach any score. |  |
| 3 | pass 2 | `mobile/lib/recommendation/types.ts:128` | correctness | S | RankedRestaurant.match.score comes from the cache while .score.compatibilityScore is recomputed, so a list can be sorted by one number and l |  |
| 3 | pass 2 | `mobile/lib/taste-vector.ts:110` | correctness | M | A user's cuisine correction never reaches the taste graph or the recommendation surfaces — only the detail screen honours it. |  |
| 3 | pass 2 | `mobile/lib/visit-payoff.ts:115` | testing | M | No test binds a SQL `returns table` column list to the TypeScript row type that reads it, so a column rename in a migration passes tsc, jest |  |
| 3 | pass 1 (unverified) | `mobile/lib/visits.ts:446` | retention-product | S | Hiding a visit deletes its feed post, so un-hiding it later can never bring the post back. |  |
| 3 | pass 1 (unverified) | `mobile/lib/visits.ts:289` | correctness | S | Changing a visit's restaurant leaves the feed post announcing the old restaurant. |  |
| 3 | pass 1 (unverified) | `mobile/lib/wrapped.ts:94` | correctness | M | The week boundary is UTC Monday in SQL and visitsSince but local Monday in isoWeekStart/analytics, so a Sunday-evening dinner (20:00–24:00 E |  |
| 3 | pass 1 (unverified) | `mobile/lib/wrapped.ts:83` | data-quality | S | When the current week has no visits yet, last week's row is shown under a hard-coded 'THIS WEEK' eyebrow and the share card says 'YOUR PALAT |  |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:213` | retention-product | S | A candidate restored after a background relaunch never gets its dwell timer re-armed |  |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:249` | correctness | S | ingest accepts any fix with accuracy >= 0, so a kilometre-accurate significant-change fix can reset a real stop |  |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:250` | cost | S | No freshness check on incoming fixes — a cached location makes a brand-new candidate born already past the dwell threshold |  |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:37` | correctness | S | preciseManager is never configured for background use and gets only 15s, so background emits likely always fall back to the ±100m coarse cen |  |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:312` | correctness | S | A second emitStop within 15s overwrites pendingPreciseEmit and the first stop is dropped with no disk record |  |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:101` | data-quality | S | The 60-entry diagnostic log is written on every location fix, so it churns away within minutes and never contains the event you need |  |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:29` | performance | L | Continuous location updates run 24/7 with pausing disabled — the most expensive non-navigation CoreLocation mode, always on |  |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitManager.swift:369` | retention-product | S | No didChangeAuthorization handler — an Always→WhenInUse downgrade leaves the monitor armed and the debug screen reporting monitoring=true | fixed on the JS side (stopMonitoring on flag off) |
| 3 | pass 2 | `mobile/modules/palate-visit-monitor/ios/PalateVisitMonitorModule.swift:46` | correctness | M | stopState() reads live detector state from the JS thread while CoreLocation mutates it on main — unsynchronised, and it touches UIKit off-ma |  |
| 3 | pass 2 | `mobile/package.json:28` | testing | M | testMatch is confined to `lib/**`, so any test placed under app/ or components/ is silently never run — and there are zero render tests for  |  |
| 3 | pass 2 | `mobile/theme.ts:14` | accessibility | S | White on the primary red is 4.09:1 — the app's main CTA fails WCAG AA |  |
| 3 | pass 2 | `mobile/theme.ts:16` | accessibility | M | colors.red is used as a text color in 66 places despite redText existing for exactly that |  |
| 3 | pass 2 | `supabase/02_policies.sql:34` | cost | S | The entire 1066-row Google Places cache is downloadable by anyone with the anon key |  |
| 3 | pass 2 | `supabase/03_functions.sql:59` | security-privacy | S | purge_old_location_events is never scheduled — raw GPS pings are retained forever — and it is PUBLIC-executable |  |
| 3 | pass 2 | `supabase/eval/run.ts:173` | testing | M | The eval's LLM pass uses a different gate than production and asserts none of the qualitative fields the LLM exists to produce |  |
| 3 | pass 2 | `supabase/functions/_shared/classifier.ts:1142` | data-quality | M | dish_family never reaches the scorer at all, and it is absent from live nearby responses, so a dish chip behaves differently depending on wh |  |
| 3 | pass 2 | `supabase/functions/_shared/classifier.ts:842` | correctness | S | Supermarkets and hypermarkets pass the eligibility gate; the NON_RESTAURANT_TYPES rule that was meant to stop them is unreachable |  |
| 3 | pass 2 | `supabase/functions/_shared/llm-classifier.ts:126` | data-quality | S | Per-field confidence picking can pair the LLM's subregion with the rules' cuisine, producing a contradictory row |  |
| 3 | pass 2 | `supabase/functions/_shared/receipt-parser.ts:210` | data-quality | S | The Square and Grubhub body fallbacks turn marketing mail into restaurant names, violating the file's own 'prefer null over a guess' rule |  |
| 3 | pass 2 | `supabase/functions/classify-cuisine-backfill/index.ts:60` | cost | S | The backfill has no attempted-marker, so rows the model abstains on are re-sent to Haiku every 10 minutes forever |  |
| 3 | pass 2 | `supabase/functions/classify-cuisine-backfill/index.ts:26` | cost | S | The backfill's classifier_version suffix permanently defeats the details cache for every row it touches |  |
| 3 | pass 2 | `supabase/functions/featured-lists-refresh/index.ts:198` | cost | M | The nightly cron runs at 04:00 UTC — just after the budget resets — and breaks out of the city loop on exhaustion with no ordering, starving |  |
| 3 | pass 2 | `supabase/functions/featured-lists-refresh/index.ts:241` | correctness | S | The freshness guard reads the newest refreshed_at across all categories, so a partially-written city is treated as fresh and its missing lis |  |
| 3 | pass 2 | `supabase/functions/featured-lists-refresh/index.ts:139` | cost | S | Any authenticated user can trigger up to 45 Google Text Searches per request with an arbitrary city_key, with no per-user rate limit |  |
| 3 | pass 2 | `supabase/functions/generate-weekly-wrapped/index.ts:39` | correctness | S | The active-user query is unpaginated and silently truncates at PostgREST's 1000-row cap, so past ~1000 visits a fortnight some users never g |  |
| 3 | pass 2 | `supabase/functions/gmail-import/index.ts:709` | data-quality | S | mealTypeFor reads local hours in a UTC runtime, so every imported US visit gets the wrong meal_type |  |
| 3 | pass 2 | `supabase/functions/gmail-import/index.ts:435` | correctness | S | handleScan returns HTTP 200 carrying an error body, so the client reports success on a failed Gmail scan; fetchMessage never checks r.ok |  |
| 3 | pass 2 | `supabase/functions/gmail-import/index.ts:643` | cost | S | gmail-import defaults GOOGLE_DAILY_CALL_CAP to 2000 while featured-lists-refresh, places-proxy and reclassify all default to 1500, against t |  |
| 3 | pass 2 | `supabase/functions/gmail-import/index.ts:158` | performance | M | handlePreview issues up to 500 sequential Gmail format=full fetches in one invocation and will be killed part-way with no partial result |  |
| 3 | pass 2 | `supabase/functions/gmail-import/index.ts:508` | security-privacy | M | The Gmail access token is still stored as plaintext in gmail_tokens.access_token, so the security claim on line 505 is not true as written |  |
| 3 | pass 2 | `supabase/functions/gmail-import/index.ts:624` | data-quality | M | visited_at is the email's arrival time, so an OpenTable confirmation dates the visit to the day the reservation was booked, not the day the  |  |
| 3 | pass 2 | `supabase/functions/gmail-import/index.ts:675` | data-quality | M | placeIdForName matches restaurants by name with no geography and no LIKE-escaping, so a receipt can be attributed to a same-named place in a |  |
| 3 | pass 2 | `supabase/functions/gmail-import/index.ts:643` | cost | S | gmail-import passes a different cap into the shared kill-switch RPC, so the effective daily budget is 1500-2000 depending on which caller la |  |
| 3 | pass 2 | `supabase/functions/notify-feed-post/index.ts:82` | correctness | S | The visibility filter is applied to the recipient's own profile_visibility instead of the poster's, so it suppresses the wrong people and ne |  |
| 3 | pass 2 | `supabase/functions/notify-feed-post/index.ts:120` | code-health | S | Three of subtitleFor's four branches are unreachable, and the comment they rest on cites a trigger that migration 0058 dropped — sharing a W |  |
| 3 | pass 2 | `supabase/functions/notify-feed-post/index.ts:82` | correctness | S | Recipients are filtered by their OWN profile_visibility, so setting your profile to private silently stops you receiving your friends' notif |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:446` | cost | S | The blurb action has no cap, no kill switch and no telemetry — it is the one paid path invisible to the cost dashboard |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:329` | cost | S | The search action has no per-user rate limit, and the nearby limit counts location_events rather than API calls |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:350` | retention-product | S | Text search is pinned to includedType "restaurant", so cafés, bakeries and bars cannot be found in the manual add-a-visit flow |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:199` | cost | M | The nearby read-through cache records coverage in one unit and tests hits in another, and is keyed on seven different client radii |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:496` | correctness | S | The density-based chain upgrade changes chain_type but leaves the eligibility that was computed from the old value |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:88` | cost | M | The server read-through cache is keyed on exact radius and needs 15 results, so the two highest-frequency paths can never hit it |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:325` | cost | S | handleSearch has no rate limit and no cache of any kind - every submit is a fresh billed Text Search |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:77` | retention-product | S | The 1500/day budget is global and unshared, so ordinary usage by ~15 users trips the kill switch and degrades everyone |  |
| 3 | pass 2 | `supabase/functions/places-proxy/index.ts:284` | correctness | M | The details endpoint - the only path that can produce enrichment - has zero callers, and a version bump would make it maximally expensive if |  |
| 3 | pass 2 | `supabase/functions/send-push/index.ts:149` | correctness | S | Deferred and retried outbox rows re-enter the queue without re-running next_sendable_at, so a retry can deliver inside the recipient's quiet |  |
| 3 | pass 2 | `supabase/migrations/0012_analytics_events.sql:30` | data-quality | S | analytics_events accepts unauthenticated inserts from anyone holding the anon key |  |
| 3 | pass 2 | `supabase/migrations/0017_sunday_wrapped_cron.sql:32` | correctness | S | The Sunday Wrapped cron has no timeout_milliseconds, so it hits the same pg_net 5s default that 0093 diagnosed and fixed only for featured-l |  |
| 3 | pass 2 | `supabase/migrations/0018_referrals.sql:61` | security-privacy | S | my_referral_stats and the population_* views leak profile aggregates past RLS to anon |  |
| 3 | pass 2 | `supabase/migrations/0024_featured_lists_cache.sql:89` | cost | M | Any signed-in user can inject unlimited cities into the nightly Google Places refresh |  |
| 3 | pass 2 | `supabase/migrations/0024_featured_lists_cache.sql:89` | cost | S | Any authenticated user can register unlimited arbitrary cities, each costing ~15 nightly Text Searches for the next 14 days |  |
| 3 | pass 2 | `supabase/migrations/0052_chain_detection.sql:79` | retention-product | S | refresh_chain_brands() runs once at migration time and is never scheduled, so every restaurant row added since 0052 has is_chain_brand = fal |  |
| 3 | pass 1 (unverified) | `supabase/migrations/0057_broadcast_activity.sql:168` | security-privacy | S | The friend-visit push is enqueued on insert regardless of is_public and is never withdrawn when the visit is hidden or deleted. |  |
| 3 | pass 2 | `supabase/migrations/0069_top_ranked_places.sql:71` | security-privacy | S | Seven SECURITY DEFINER readers are still executable by anon; only in-body guards stop them |  |
| 3 | pass 2 | `supabase/migrations/0072_feed_event_visit_link.sql:21` | retention-product | M | A feed post with visit_id null escapes the curation gate and is the row anon can read |  |
| 3 | pass 2 | `supabase/migrations/0093_turn_on_server_push.sql:129` | correctness | M | No http cron reads back net._http_response, so a 401 or timeout from any scheduled edge call is invisible — the failure mode 0093 spent a da |  |
| 3 | pass 2 | `supabase/migrations/0094_comeback_push.sql:35` | data-quality | S | The comeback push says "Not since Tuesday" for a visit up to 45 days old and "Not since March" with no year for anything older, and has no o |  |
| 3 | pass 2 | `supabase/migrations/0094_comeback_push.sql:35` | data-quality | S | The comeback push says "Not since Tuesday" for a visit up to 45 days old. |  |
| 3 | pass 2 | `supabase/migrations/0095_place_heat.sql:117` | data-quality | S | place_heat counts all-time saves and likes with no time window, so a place wishlisted months ago renders as "lit up this week". |  |
| 3 | pass 2 | `supabase/migrations/0095_place_heat.sql:63` | data-quality | M | The nightly rating snapshot writes ~1k rows/night forever to power a 'velocity' regime that is structurally unreachable for almost every row |  |
| 3 | pass 2 | `supabase/migrations/0095_place_heat.sql:113` | performance | S | place_heat runs four correlated subqueries per candidate against visits, wishlist and feed_events, none of which has an index on the column  |  |
| 3 | pass 2 | `supabase/migrations/0097_cuisine_from_google_types.sql:47` | data-quality | M | 0097/0091 map ten Google types the TypeScript CUISINE_TYPE_MAP does not know, and contradict it on cuban_restaurant |  |
| 3 | pass 2 | `supabase/migrations/0100_llm_cuisine_backfill_cron.sql:13` | cost | S | A one-off approved ~$0.50 backfill is scheduled as a permanent every-10-minutes job with no end condition |  |

Severity 1–2 findings (95) are in the journals and not listed here.

## Attack pass on "Not interested" — confirmed holes

- [3] `mobile/lib/featured-lists.ts:159` — Featured Lists (Discover carousel + /featured-list/[slug], the target of the Friday/Saturday discovery pings) never apply the hidden set. buildFeaturedLists cal — **closed**
- [3] `mobile/app/(tabs)/discover.tsx:434` — Discover's mood-chip catalogue fallback (cuisineCandidates/dishCandidates when the nearby pool lacks the cuisine) is not filtered by the hidden set; the rows ar — **closed**
- [3] `mobile/components/RecommendationsCard.tsx:203` — In-session staleness: invalidatePersonalSignal() nulls the cache but no rendering component reloads on it. RecommendationsCard (Home) and StretchPick load on [l — **closed**
- [2] `supabase/functions/group-recs/index.ts:125` — group-recs never reads place_dislikes: a place any member marked Not interested can be the group's #1 pick. Candidates come from restaurants_resolved filtered o — **closed**
- [2] `mobile/lib/similar-restaurants.ts:79` — 'Find more like X' (/similar/[place_id]) gates with isRecommendable(r) and no hidden set; the similar_restaurants RPC (0029) only excludes the caller's visited  — **closed**
- [2] `mobile/components/HypeMap.tsx:48` — Feed HypeMap renders placeHeat() output straight to markers and the 3-row legend (names, tap-through to the place) with no hidden filter; place_heat (0095, secu — **closed**
- [2] `mobile/lib/palate/pairCompatibility.ts:142` — Friend compatibility's 'spots you'd both like nearby' builds myGraph with assembleGraph(myVector, null), so graph.dislikes is EMPTY_DISLIKES and generateCandida — **closed**
- [2] `mobile/lib/personal-signal.ts:125` — The exclusion fails open and the failure is cached: `listDislikes().catch(() => [])` turns any error on the place_dislikes read (offline at launch, transient 5x — **closed**
- [2] `mobile/app/(tabs)/discover.tsx:656` — The search-bar suggestion panel 'ALL RESTAURANTS NEARBY' (loadCityRestaurants, top 100 by review count from restaurants_resolved) applies neither the hidden set — **closed**
- [2] `mobile/lib/recommendation/compatibility.ts:252` — COMMITTED: the dampening share is keyed on cuisine_subregion (else cuisine_region) while the lesson's dominant 14-pt component is keyed on cuisine_type. The Tas — **closed**
- [2] `mobile/lib/recommendation/types.ts:24` — COMMITTED + LIVE: the dish-family part of the 'food' lesson (10-pt weight; the only piece that generalises 'burgers' across cuisines or separates sushi from ram — **closed**
- [2] `mobile/app/(tabs)/discover.tsx:909` — On Discover the hide is only a per-card `dismissed` flag inside RestaurantCompatibilityCard; discover.tsx passes no onDismissed and never updates allNearby or p — **closed**
